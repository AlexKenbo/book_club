# План реализации: Кастомный OTP через SMSC (Вариант 1)

## ✅ Текущее состояние

Ваш проект **уже частично готов** для работы с кастомной аутентификацией:

1. ✅ Таблица `otp_codes` создана (`002_custom_auth.sql`)
2. ✅ Edge Functions `request-otp` и `verify-otp` реализованы
3. ✅ `verify-otp` генерирует JWT с `SUPABASE_JWT_SECRET`
4. ✅ Клиент (`supabaseClient.ts`) передает токен в заголовках
5. ✅ RxDB репликация использует Supabase клиент с токеном

## 📋 Что нужно сделать

### Шаг 1: Получить SUPABASE_JWT_SECRET

1. Откройте [Supabase Dashboard](https://app.supabase.com) → ваш проект
2. **Settings** → **API** → **JWT Settings**
3. Скопируйте **JWT Secret** (длинная строка)

⚠️ **Не коммитьте этот секрет в Git!**

### Шаг 2: Установить секреты

```bash
# Локальная разработка
supabase secrets set \
  SUPABASE_URL="ваш-supabase-url" \
  SUPABASE_SERVICE_ROLE_KEY="ваш-service-role-key" \
  SUPABASE_JWT_SECRET="ваш-jwt-secret-из-dashboard" \
  SMSC_LOGIN="ваш-логин" \
  SMSC_PASSWORD="ваш-пароль" \
  SMSC_SENDER="BookClub" \
  SMSC_TEST="true"  # для тестирования без реальной отправки SMS
```

### Шаг 3: Применить миграции

```bash
# Применить все миграции (включая новые RLS политики)
npx supabase db reset
```

Новые миграции:
- `003_rls_custom_jwt.sql` - RLS политики для кастомного JWT
- `004_sessions_table.sql` - таблица sessions (опционально)

### Шаг 4: Проверить работу

1. Запустите приложение: `npm run dev`
2. Перейдите на страницу входа
3. Введите номер телефона
4. Если `SMSC_TEST=true`, код будет в логах функции
5. Введите код и войдите

## 🔍 Как это работает

### Архитектура

```
┌─────────────┐
│   Клиент    │
│  (React)    │
└──────┬──────┘
       │
       │ 1. request-otp (phone)
       ▼
┌─────────────────┐
│  Edge Function  │
│   request-otp    │
└──────┬──────────┘
       │
       │ 2. Генерирует OTP, отправляет SMS
       ▼
┌─────────────────┐
│   otp_codes      │
│   (Postgres)     │
└─────────────────┘

┌─────────────┐
│   Клиент    │
│  (React)    │
└──────┬──────┘
       │
       │ 3. verify-otp (phone, code)
       ▼
┌─────────────────┐
│  Edge Function  │
│   verify-otp    │
└──────┬──────────┘
       │
       │ 4. Проверяет OTP, создает профиль
       │ 5. Генерирует JWT с SUPABASE_JWT_SECRET
       ▼
┌─────────────────┐
│   profiles      │
│   (Postgres)    │
└─────────────────┘
       │
       │ 6. Возвращает JWT токен
       ▼
┌─────────────┐
│   Клиент    │
│  (React)    │
└──────┬──────┘
       │
       │ 7. Сохраняет токен в localStorage
       │ 8. Использует токен в Authorization header
       ▼
┌─────────────────┐
│  Supabase API   │
│  (PostgREST)    │
└──────┬──────────┘
       │
       │ 9. Проверяет JWT, применяет RLS
       ▼
┌─────────────────┐
│   Postgres      │
│   (с RLS)       │
└─────────────────┘
```

### JWT Структура

Ваш JWT токен содержит:
```json
{
  "role": "authenticated",
  "aud": "authenticated",
  "sub": "+79001234567",  // номер телефона = user_id
  "phone": "+79001234567",
  "app_metadata": { "provider": "smsc" },
  "user_metadata": { "name": "+79001234567" },
  "iss": "supabase",
  "iat": 1234567890,
  "exp": 1234567890
}
```

### RLS Политики

После применения миграции `003_rls_custom_jwt.sql`:

- **Функция `get_user_id()`**: Извлекает `sub` из `request.jwt.claims`
- **Profiles**: Чтение публичных, изменение только своих
- **Books**: Чтение всех, изменение только своих
- **Requests**: Чтение только своих (как lender или borrower)

### RxDB Репликация

RxDB использует `replicateSupabase`, который передает Supabase клиент.
Клиент автоматически добавляет токен в заголовки:
```javascript
headers.Authorization = `Bearer ${session.token}`
```

Поэтому репликация работает без изменений! ✅

## ✅ Преимущества варианта 1

1. ✅ **Минимальные изменения** - RxDB репликация работает как есть
2. ✅ **RLS работает** - политики используют `request.jwt.claims->>'sub'`
3. ✅ **Realtime работает** - токен передается через `realtime.setAuth()`
4. ✅ **PostgREST работает** - токен в заголовке `Authorization`
5. ✅ **Совместимость** - JWT подписан тем же секретом, что и Supabase Auth

## 🐛 Отладка

### Проверить токен

```javascript
// В консоли браузера
const token = localStorage.getItem('libshare_access_token');
const payload = JSON.parse(atob(token.split('.')[1]));
console.log('JWT payload:', payload);
```

### Проверить RLS

```sql
-- В Supabase SQL Editor
select current_setting('request.jwt.claims', true)::json->>'sub' as user_id;
```

### Временно отключить RLS

```sql
alter table profiles disable row level security;
alter table books disable row level security;
alter table requests disable row level security;
```

## 📚 Документация

- [CUSTOM_AUTH_QUICKSTART.md](./CUSTOM_AUTH_QUICKSTART.md) - быстрый старт
- [CUSTOM_AUTH_SETUP.md](./CUSTOM_AUTH_SETUP.md) - полная документация

## 🎯 Итог

**Вариант 1 полностью готов к использованию!** 

Нужно только:
1. Получить `SUPABASE_JWT_SECRET` из Dashboard
2. Установить секреты через `supabase secrets set`
3. Применить миграции через `npx supabase db reset`

После этого всё будет работать: аутентификация через SMSC, RLS, RxDB репликация, Realtime. 🚀
