# 🚀 Быстрый старт

## Предварительные требования
- Node.js 18+.
- Docker, если планируете локальный Supabase.

## Запуск приложения
1. Установите зависимости:

```bash
npm install
```

2. Запустите приложение:

```bash
npm run dev
```

3. Откройте `http://localhost:3000`.

## Демо‑режим
Если Supabase не настроен, можно включить демо‑режим на экране входа. Он создаст локальные данные и позволит посмотреть основные сценарии.

## Supabase за 2 минуты
Вариант A: Supabase Cloud.
Ключи уже прописаны в `lib/supabaseClient.ts`. Если используете свой проект, замените `SUPABASE_URL` и `SUPABASE_ANON_KEY`.

Вариант B: локальный Supabase.
1. Запустите: `npx supabase start`.
2. Скопируйте `API URL` и `anon key` из вывода.
3. Обновите `SUPABASE_URL` и `SUPABASE_ANON_KEY` в `lib/supabaseClient.ts`.
4. Примените миграции: `npx supabase db reset`.

## Кастомная аутентификация (OTP через SMSC)
Подробная настройка находится в документах:
- `docs/CUSTOM_AUTH_QUICKSTART.md`
- `docs/CUSTOM_AUTH_SETUP.md`

## Команды
- `npm run dev` — запуск dev‑сервера.
- `npm run build` — production‑сборка.
- `npm run preview` — предпросмотр production‑сборки.
- `npx supabase start` — локальный Supabase.
- `npx supabase db reset` — применение миграций.

## Документация
- `README.md` — основная документация.
- `docs/INSTALLATION.md` — полная установка.
- `SUPABASE_README.md` — локальный Supabase и функции.
